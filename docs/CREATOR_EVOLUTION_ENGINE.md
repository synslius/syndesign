# Creator Evolution Engine

This is the Darwin layer Nolan meant: not a dashboard, not a pretty board, and not a static gallery.

DASH should self-evolve creator workflows. The repo stays minimal because every new primitive must survive selection pressure before it becomes package code.

```text
observe -> propose mutations -> render candidates -> evaluate -> select -> retain -> regress
```

## Core idea

A creator workflow is treated like an organism:

| Darwin term | DASH meaning |
|---|---|
| population | competing workflow/tool-adapter candidates |
| mutation | one narrow change to schema, prompt DNA, adapter, layout, proof, or command |
| phenotype | the actual rendered artifact or runnable workflow |
| fitness | creator-usefulness score backed by commands and visual QA |
| selection | keep the candidate that improves usefulness without growing the core |
| retention | promote repeated winners into docs/scripts/packages |
| regression | prove the winner did not break existing surfaces |

The key is pressure. A mutation does not count because it sounds clever. It counts only if it helps creators make, verify, or remix something better.

## Evolution loop

1. **Observe**
   - Inspect a real creator job, failed render, friction point, or repeated agent move.
   - Store only the public-safe pattern, not private source material.

2. **Generate mutations**
   - Create 3-5 small candidates.
   - Change exactly one dimension per candidate: surface, grammar, proof, adapter, command, or schema.
   - Do not add a package until repeated examples prove the primitive.

3. **Render phenotypes**
   - Every candidate needs a visible artifact or executable workflow.
   - Acceptable phenotypes: HTML board, p5 sketch, contact sheet, PDF, Remotion/Manim render, TouchDesigner screenshot/tox note, skill prompt pack.

4. **Evaluate fitness**
   - Score with creator-facing criteria, not vanity metrics:
     - Does it reduce time-to-first-artifact?
     - Does it preserve creative memory?
     - Does it make remix safer?
     - Does it provide proof before publish?
     - Does it avoid private leakage?
     - Does it keep the core smaller than the edge?

5. **Select**
   - Keep the smallest candidate that improves the creator job.
   - Reject dashboard-only output unless the dashboard directly changes creator behavior.

6. **Retain**
   - If a pattern wins once, keep it as an example or usecase.
   - If it wins repeatedly, add a script.
   - If scripts repeat across surfaces, promote a package primitive.
   - If an agent move repeats, promote a skill.

7. **Regress**
   - Run score gates and visual/browser proof.
   - A new frontier adapter must not break document, canvas, p5, video, creator capsule, or public-boundary gates.

## Minimal file contract

A self-evolution contribution needs only this:

```text
examples/creator-evolution-loop.json
  one public-safe evolution runbook

scripts/creator-evolution-check.ts
  machine gate for the runbook and doctrine

docs/CREATOR_EVOLUTION_ENGINE.md
  the operating model
```

Everything else is optional until the loop proves it deserves more structure. The first retained operational layer is the mutation candidate ledger: `examples/creator-mutation-candidates.json` plus `bun creator:mutation-check`.

## Retained mutations

- [`poster-surface-route`](../usecases/creator/creator-poster-surface.md): first actual creator surface mutation; converts the creator capsule into a measured/printable poster proof while preserving intent, memory, boundary, proof, and remix rule.
- [`adapter-prompt-dna-route`](../usecases/creator/creator-prompt-dna-adapter.md): first frontier model adapter mutation; preserves prompt DNA and preview policy without committing raw generated media.
- [`creator-motion-storyboard-route`](../usecases/creator/creator-motion-storyboard.md): bridge from capsule memory to video direction; keeps motion as a six-frame fixed-canvas contract until contact-sheet QA exists.
- [`creator-social-card-route`](../usecases/creator/creator-social-card.md): post-ready social/export mutation; turns capsule memory into a crop-safe 1200x630 card with measure, print, browser QA, and no raw media.
- [`creator-p5-sketch-route`](../usecases/creator/creator-p5-sketch.md): creative-coding adapter mutation; turns capsule memory into deterministic `dash-flow-field` frame probes and a fixed preview before any p5 runtime or raw rendered media.
- [`creator-remotion-scene-route`](../usecases/creator/creator-remotion-scene.md): timeline-code adapter mutation; turns capsule memory into a generated ignored Remotion TSX composition stub plus fixed-canvas proof before any Remotion runtime or rendered video.
- [`creator-manim-scene-route`](../usecases/creator/creator-manim-scene.md): explainer-scene adapter mutation; turns capsule memory into a generated ignored Manim Python scene stub plus fixed-canvas proof before any renderer dependency or MP4 output.
- [`creator-touchdesigner-tox-route`](../usecases/creator/creator-touchdesigner-tox.md): live-visual-network adapter mutation; turns capsule memory into generated ignored TouchDesigner topology/twozero safety artifacts plus fixed-canvas proof before any local port, screenshot, `.tox/.toe`, raw media, or live TD connection.
- [`creator-browser-demo-route`](../usecases/creator/creator-browser-demo.md): browser-native prototype mutation; turns capsule memory into a self-contained clickable local HTML phenotype with interaction smoke, fixed-canvas proof, and deploy rollback boundary before any backend, analytics, screenshot, hosting config, or public URL.
- [`creator-pdf-zine-route`](../usecases/creator/creator-pdf-zine.md): printable process-surface mutation; turns capsule memory into a six-panel one-sheet HTML/PDF zine with measure, print, browser QA, and no raw media or private workshop notes.
- [`creator-panel-token-alignment-route`](../usecases/creator/creator-panel-token-alignment.md): CSS token --panel alignment mutation (PR #80); aligns 3 warm-paper surfaces (pdf-zine, poster-surface, social-card) that defined --panel in :root but never used it in body CSS. These surfaces had hardcoded warm-paper rgba values (rgba(255,249,235,.72), rgba(255,248,235,.56), rgba(255,248,234,.72)) that are visually equivalent to --panel consensus rgba(244,234,214,0.06). Now all 6 warm-paper surfaces consistently use var(--panel). All gates MAXXED (308/316 hackathon, 11/11 measure+print, mutation-check, cache-integrity).
- [`creator-paper-token-alignment-route`](../usecases/creator/creator-panel-token-alignment.md): warm-paper family --paper token consensus #f2eadb (PR #73); aligns drifted surfaces (pdf-zine, poster-surface, social-card, p5-sketch) to shared family texture with measure+print verification + updated QA evidence. Part of creator-surface-consistency-phase-a baseline.
- [`creator-vision-qa-standardization`](../scripts/creator-touchdesigner-tox-check.ts): cross-surface proof mutation (PRs #30-#42); extends every retained creator route with standardized browser DOM/vision QA evidence (provider-fallback pattern, needle-aligned .artifacts QA, real browser_console DOM verification) so autonomous Darwin runs produce verifiable visual proof across all 10 surfaces without requiring multimodal vision API support.
- [`creator-regression-orchestrator-route`](../scripts/creator-regression-check.ts): unified regression gateway mutation (PR #44); adds `bun creator:regression` that runs all 16 surface + autonomy + mutation + capsule + evolution + hackathon checks in one pass with per-surface PASS/FAIL, timing, and aggregate verdict — making autonomous Darwin loop evaluation faster and more reliable.
- [`creator-darwin-autonomy-route`](../scripts/creator-darwin-autonomy-check.ts): Darwin autonomy pre-flight mutation; adds `bun creator:darwin-autonomy-check` that validates repo anchor (correct repo + remote), git cleanliness (reports uncommitted changes), MCP connectivity (research_vault), and baseline gates (creator:mutation-check) before autonomous cron slices execute — directly addressing wrong-repo-anchor, dirty-worktree, and MCP-gap failure modes documented in hermes-gsd-evolution pitfalls.
- [`creator-multi-surface-proof-route`](../scripts/creator-multi-surface-proof.ts): unified artifact-measurement gateway mutation (PR #45); adds `bun creator:multi-surface-proof` that validates all 11 retained surface HTML artifacts against measure:check + print:render in one pass with per-surface PASS/FAIL and timing — catching overflow/render drift across surfaces before visual QA.
- [`creator-playwright-health-route`](../scripts/creator-playwright-health-check.ts): Playwright Chromium binary health pre-flight mutation; adds `bun creator:playwright-health-check` that validates ms-playwright cache directory, chromium_headless_shell binary, and a real measure:check smoke test before any autonomous Darwin slice runs — preventing silent 0/11 measure+print failures from binary version drift (documented in hermes-gsd-evolution pitfalls). Wired as the first check in the regression orchestrator.
- [`creator-docs-surface-parity-route`](../scripts/creator-docs-surface-parity.ts): Creator docs-surface parity mutation (PR #49); adds `bun creator:docs-surface-parity` that cross-references WORKFLOW_INDEX.md, CREATOR_EVOLUTION_ENGINE.md, mutation-candidates.json, examples/*.html, multi-surface-proof SURFACES, and package.json scripts in one pass — catching doc/ledger/filesystem drift before it accumulates across autonomous Darwin runs. Exit 0 for 0-3 informational warnings, exit 1 for 4+ actionable issues.
- [`creator-cron-slice-health-route`](../scripts/creator-cron-slice-health.ts): Creator cron slice health mutation; adds `bun creator:cron-slice-health` that queries GitHub for the most recent merged Darwin PR, verifies CI status on the merge commit, and reports slice completion evidence (branch, SHA, PR URL, merge status, CI verdict) — validating that the most recent autonomous Darwin cron slice completed cleanly. Exit 0 healthy, exit 1 unhealthy, exit 2 no-Darwin-PR (first-run). Wired into the regression orchestrator after the autonomy check.
- [`creator-agent-context-route`](../scripts/creator-agent-context-health.ts): Agent context health mutation; adds `bun creator:agent-context-health` that validates AGENTS.md, WORKFLOW_INDEX.md, CREATOR_EVOLUTION_ENGINE.md, and mutation-candidates.json are present, parseable, and structurally sound (required sections, valid JSON, 3-5 candidates, exactly one selected winner) — ensuring any autonomous agent can start work without missing or corrupted docs. All local-only; no network, secrets, or external dependencies.
- [`creator-surface-health-route`](../scripts/creator-surface-health.ts): Per-surface health mutation; adds `bun creator:surface-health` that dynamically discovers all `examples/creator-*.html` surfaces, maps each to its corresponding `creator:*check` script via a SURFACE_CHECK_MAP, runs every individual surface gate, and reports per-surface PASS/FAIL with timing — catching broken individual surfaces, missing check scripts for new surfaces, and surfaces present in examples/ but absent from the regression orchestrator. 11/11 surfaces healthy in 216ms. Wired into the regression orchestrator after agent-context-health.
- [`creator-script-health-route`](../scripts/creator-script-health.ts): Creator script source file health mutation; adds `bun creator:script-health` that reads package.json scripts, filters for creator:* entries, extracts source file paths (handling direct paths, through-run paths, and arguments), verifies each referenced source file exists, and reports PASS/FAIL per entry — catching stale script entries pointing to deleted or renamed files before they cause silent failures in autonomous Darwin runs. 23/23 healthy. Wired into the regression orchestrator after surface-health.
- [`creator-orphan-script-route`](../scripts/creator-orphan-script.ts): Orphan script detection mutation (PR #57); adds `bun creator:orphan-script` that discovers all `scripts/creator-*.ts` files and cross-references against package.json `creator:*` entries — catching orphan source files that exist on the filesystem but have no package.json entry. Inverse of creator-script-health: together they enforce perfect 1:1 correspondence between scripts and package.json entries. 26/26 matched, 0 orphans. Wired into the regression orchestrator after script-health.
- [`creator-workflow-gate-health-route`](../scripts/creator-workflow-gate-health.ts): Creator WORKFLOW_INDEX gate health mutation; adds `bun creator:workflow-gate-health` that parses WORKFLOW_INDEX.md for all `bun <script>` references, cross-references against package.json scripts, runs each gate (with special handling for arg-requiring and long-running scripts), and reports PASS/MISSING/FAIL per gate — catching doc drift where WORKFLOW_INDEX describes gates that are absent or broken. 32/32 gates exist and pass. Wired into the regression orchestrator after script-health.
- [`creator-example-check-coverage-route`](../scripts/creator-example-check-coverage.ts): Example surface check coverage audit mutation; adds `bun creator:example-check-coverage` that discovers all `examples/creator-*.html` surfaces and verifies each has a corresponding `creator:*check` script in `package.json` — catching surfaces added without machine gates and orphan check scripts without surfaces. Uses known mapping table for 3 non-standard naming exceptions (poster-surface→poster-check, prompt-dna-adapter→prompt-dna-check, frontier-capsule→capsule-check) with auto-derivation for standard surfaces. 11/11 surfaces covered, 0 orphans. Wired into the regression orchestrator after workflow-gate-health.
- [`creator-post-check-coverage-route`](../scripts/creator-post-check-coverage.ts): QA evidence coverage audit mutation; adds `bun creator:post-check-coverage` that discovers all `examples/creator-*.html` surfaces, maps each to its expected `.artifacts/creator-*-qa.md` evidence file (with known mapping for non-standard naming: poster drops "surface", capsule excluded), verifies QA files exist and contain real browser evidence markers (browser_console, DOM verification, screenshot_path, canvas), and reports per-surface PASS/MISS/WEAK — catching surfaces where QA drift leaves stale or missing evidence files. Now 11/11 surfaces audited, 0 gaps (creator-prompt-dna-qa-route closed the last gap). Wired into the regression orchestrator after example-check-coverage.
- [`creator-regression-duration-route`](../scripts/creator-regression-duration.ts): Regression duration tracker mutation; adds `bun creator:regression-duration` that reads the timing manifest written by the regression orchestrator after each run (`examples/creator-regression-timing.json`), compares per-check durations against stored running-average baselines (`examples/creator-regression-duration-baseline.json`), and flags any check exceeding 2x baseline threshold — catching slow performance regressions in Darwin checks before they accumulate. First run auto-creates baseline. Wired into the regression orchestrator after post-check-coverage.
- [`creator-prompt-dna-qa-route`](../scripts/creator-prompt-dna-check.ts): Prompt DNA adapter QA evidence mutation; extends `bun creator:prompt-dna-check` to generate standardized `.artifacts/creator-prompt-dna-adapter-qa.md` with VISION_QUESTION_TEMPLATE + real browser_console DOM verification + robust needle alignment + vision QA provider fallback — closing the last QA evidence gap across all 11 creator surfaces. All 11 retained surfaces now have standardized QA evidence.
- [`creator-measure-print-cache-route`](../scripts/creator-measure-print-cache.ts): Measure-print content-hash cache mutation; adds `bun creator:measure-print-cache` that wraps measure:check + print:render with SHA-256 content-hash caching — 11 surfaces validated in 4ms (cached) vs 26.6s (fresh), 6600x speedup for repeat creator runs. Supports `--no-cache` for forced fresh runs. Cache stored at `.cache/measure-print/` (gitignored). Wired into the regression orchestrator after regression-duration.
- [`creator-cache-integrity-route`](../scripts/creator-cache-integrity.ts): Cache integrity validation mutation; adds `bun creator:cache-integrity` that cross-checks every cached measure-print entry's SHA-256 against current source file content — catching stale cache entries when surfaces are modified but the cache wasn't invalidated. 11/11 valid on first run. Handles missing cache directory (exit 2), corrupt entries, orphaned sources, and hash mismatches. Wired into the regression orchestrator after measure-print-cache.
- [`creator-surface-consistency-route`](../scripts/creator-surface-consistency.ts): Visual grammar audit mutation; adds `bun creator:surface-consistency` that extracts CSS custom properties from `:root` blocks across all 11 creator surfaces, identifies visual families (warm-paper, dark-industrial, creative-hybrid), cross-references token names and values, and reports naming inconsistencies and value drift. Exit 0 for informational audit, exit 1 if any surface has no CSS custom property layer. Revealed: 0 shared tokens, 12 diverged, 20 unique, 1 surface with no token layer. Wired into regression orchestrator after cache-integrity. This is the first diagnostic slice for NorthStar creator-surface-consistency — aesthetic coherence, not mechanical uniformity.
- [`creator-token-alignment-route`](../examples/creator-motion-storyboard.html): CSS token alignment mutation (PR #64); aligns 7 custom properties on `creator-motion-storyboard.html` to warm-paper family consensus (`--paper:#f2eadb`, `--ink:#15120e`, `--muted:#6f6659`, `--line:rgba(21,18,14,0.18)`, `--blue:#1d4ed8`, `--amber:#c47a1f`, `--green:#2f6f4e`). All gates MAXXED (316/316 hackathon, 11/11 measure+print, typecheck, security, docs). First concrete repair mutation — moves from diagnostic to alignment. All token references use `var()` so change propagated without structure edits.
- [`creator-family-dna-artifact-route`](../examples/creator-family-dna.html): Visual grammar family DNA reference mutation; creates `examples/creator-family-dna.html` — a fixed-canvas 1684×1191 HTML reference showing 3 visual families (warm-paper with 6 surfaces, dark-industrial with 2, creative-hybrid with 1) with rendered token swatches, consensus values, and a 4-principle capsule grammar card defining DASH visual DNA. Adds `bun creator:family-dna-check` (20/20 PASS) wired into regression orchestrator and multi-surface-proof (12/12 measure+print). All gates MAXXED (316/316). This is the human-readable companion to creator:surface-consistency — the target that machine checks measure convergence against.
- [`creator-surface-convergence-route`](../scripts/creator-surface-consistency.ts): Value-level convergence tracking mutation; extends `bun creator:surface-consistency` with per-diverged-token distinct value counts (e.g., `--line` has 10 distinct values across 11 surfaces), convergence delta computation from prior run JSON baseline, and machine-readable report (`examples/creator-surface-convergence.json`). First run establishes baseline: 14 diverged tokens, 0 shared, 12 surfaces audited. Enables autonomous Darwin runs to track whether alignment slices reduce drift or stall — per-token convergence/divergence/stall icons in human-readable output. Additive to existing audit; no runtime inflation.
- [`creator-touchdesigner-token-layer-route`](../examples/creator-touchdesigner-tox.html): TouchDesigner CSS token layer mutation (PR #68); adds 18 CSS custom properties to the last surface without a `:root` block — replacing 25 hardcoded hex/rgba values with 29 `var()` references while preserving exact visual output. Token names follow DASH family conventions (`--night`, `--paper`, `--signal`, `--accent`, `--line`, `--muted`, `--panel`) with surface-specific extensions. All gates pass (measure:check, touchdesigner-tox-check, cache-integrity). Closes the last no-token-layer gap: surface-consistency now reports 0/12 surfaces without CSS token layer. Second concrete token-alignment step — moves from diagnostic to universal token participation.
- [`creator-line-token-alignment-route`](../examples/creator-pdf-zine.html): CSS --line token alignment mutation (PR #70); aligns the `--line` custom property across the warm-paper family toward consensus `rgba(21,18,14,0.18)` — fixes pdf-zine (22,19,14→21,18,14) + `--line-strong` base, poster-surface alpha (.16→0.18), and normalizes prompt-dna/social-card whitespace. p5-sketch retains `#35584c` for creative contrast. `--line` converges from 11→8 distinct values across surfaces. All gates MAXXED (316/316 hackathon, 11/11 cache-integrity, 4/4 surface checks). Third concrete token-alignment step — first cross-surface convergence of a single token.
- [`creator-amber-token-alignment-route`](../examples/creator-manim-scene.html): warm-paper family --amber token alignment mutation (PR #106); aligns manim-scene `#d4a373` → `#c47a1f` to warm-paper consensus. `--amber` converges from 2→1 distinct values across warm-paper surfaces (7 surfaces now at #c47a1f). All gates pass (measure:check, manim-scene-check, cache-integrity). Fourth concrete token-alignment step — full warm-paper convergence on --amber.
- [`creator-muted-token-alignment-route`](../examples/creator-pdf-zine.html): CSS --muted token alignment mutation (PR #71); aligns the `--muted` custom property across the warm-paper family toward consensus `#6f6659` — pdf-zine (`#6f6558`→`#6f6659`), poster-surface (`#6f675b`→`#6f6659`), prompt-dna (`#625a50`→`#6f6659`), frontier-capsule (`#746d63`→`#6f6659`). `--muted` converges from 10→6 distinct values across 12 surfaces. 7 surfaces now use consensus. p5-sketch creative intent preserved (`#6f6a5f`, 1-hex-digit divergence). Dark-industrial + creative-hybrid divergence intentional. All gates MAXXED (316/316 hackathon, 11/11 cache-integrity, 4/4 surface checks). Fourth concrete token-alignment step — second cross-surface convergence.
- [`creator-muted-surface-explore-route`](../examples/creator-muted-surface-diagnostic.html): --muted family signature diagnostic mutation (PR #112); confirms warm-paper --muted already converged (`#6f6659` on 10 surfaces). 5 outlier values are intentional family signatures: dark-industrial (browser-demo `#94a69f`, remotion-scene `#7a7164`, touchdesigner-tox `#7ea0ac`), creative-hybrid (manim-scene `#8b9ab1`, p5-sketch `#6f6a5f`). No surface edits needed — convergence complete for warm-paper family. Diagnostic phenotype documents --muted family grammar across all 11 surfaces with visual swatches and family badges.
- [`creator-amber-token-alignment-route`](../examples/creator-social-card.html): CSS --amber token alignment mutation (PR #78); aligns social-card (`#c57a1d`→`#c47a1f`) and prompt-dna (`#b86b17`→`#c47a1f`) to warm-paper consensus `#c47a1f` — `--amber` converges from 4→3 distinct values across warm-paper family. browser-demo `#ff9f6e` and manim-scene `#d4a373` retain intentional creative-hybrid divergence. All surface gates pass (social-card, prompt-dna, poster). Fifth concrete token-alignment step — third cross-surface convergence of a single token.
- [`creator-blue-token-alignment-route`](../examples/creator-manim-scene.html): CSS --blue token alignment mutation (PR #81); aligns manim-scene `--blue` from `#8db4ff` (creative-hybrid sky blue) to warm-paper consensus `#1d4ed8`. `--blue` converges from 2→1 distinct value across warm-paper family (7 surfaces). var(--blue) body references preserved (3 usages, no structure change). All gates MAXXED (28/31 regression, 11/11 cache-integrity, measure+print PASS). Sixth concrete token-alignment step — fourth cross-surface convergence of a single token.
- [`creator-ink-token-alignment-route`](../examples/creator-pdf-zine.html): CSS --ink token alignment mutation (PR #75); aligns pdf-zine (`#16130e`→`#15120e`) and frontier-capsule (`#161411`→`#15120e`) to warm-paper consensus `#15120e` — `--ink` converges from 6→5 distinct values across warm-paper family. p5-sketch `#171713` and browser-demo `#111318` retain intentional creative-hybrid and dark-industrial divergence. All gates MAXXED (31/31 regression, 11/11 cache-integrity). Seventh concrete token-alignment step — fifth cross-surface convergence.
- [`creator-surface-health-extension-route`](../scripts/creator-surface-health.ts): Surface health SURFACE_CHECK_MAP extension mutation; adds `creator-family-dna.html` to the surface health check mapping, completing 12/12 surface coverage. Wires into regression orchestrator. All 12/12 surfaces now covered in per-surface health checks.
- [`creator-accent-token-alignment-route`](../examples/creator-poster-surface.html): CSS --accent token alignment mutation (PR #79); aligns 8 warm-paper + creative-hybrid surfaces to warm-paper consensus `#ffb86b` — poster-surface, pdf-zine, social-card, motion-storyboard, prompt-dna, family-dna, p5-sketch, manim-scene. browser-demo `#ff9f6e` and frontier-capsule `#1d4ed8` retain intentional family divergence. `--accent` converges from 3→2 distinct values. Eighth concrete token-alignment step — sixth cross-surface convergence.
- [`creator-regression-noise-route`](../scripts/creator-regression-check.ts): Regression noise filter stub; deferred — regression currently clean, no pre-existing bun-types lint noise to suppress. May become relevant if future bun-types updates introduce persistent pre-existing noise.
- [`creator-panel-alpha-explore-route`](../examples/creator-surface-convergence.json): Diagnostic exploration mutation (2026-05-22); reveals warm-paper --panel already converged (rgba(244,234,214,0.06) on 6 surfaces: family-dna, motion-storyboard, pdf-zine, poster-surface, prompt-dna, social-card). 0 warm-paper divergence. Remaining 6 distinct values are intentional family divergence (dark-industrial, frontier-capsule). No surface edits needed — diagnostic closed the candidate.
- [`creator-paper-hot-alignment-route`](../examples/creator-pdf-zine.html): CSS --paper-hot token alignment mutation (PR #85); aligns pdf-zine (`#fff9eb`→`#fff8ea`) to social-card consensus `#fff8ea` — --paper-hot converges from 2→1 distinct value across warm-paper family. Both surfaces use --paper-hot for highlight/hover gradient states. Ninth concrete token-alignment step — seventh cross-surface convergence.

- [`creator-green-token-alignment-route`](../examples/creator-manim-scene.html): CSS --green token alignment mutation (PR #90); aligns manim-scene `--green` from `#9bd6ad` to warm-paper consensus `#2f6f4e`. `--green` converges from 3→2 distinct values across warm-paper family. p5-sketch `#10291f` retains intentional creative-hybrid divergence. Tenth concrete token-alignment step — eighth cross-surface convergence.
- [`creator-spacing-grammar-audit-route`](../scripts/creator-surface-consistency.ts): Spacing grammar diagnostic mutation (2026-05-22); extends `creator:surface-consistency` with `extractHardcodedSpacing()` function that audits gap/padding/margin px values across all 11 surfaces. Reveals: all 11 surfaces use hardcoded px values, zero spacing tokens. warm-paper surfaces show 3-5 distinct gap values each. spacing_audit data added to `creator-surface-convergence.json`. No surface edits — diagnostic phenotype confirms the opportunity. Next: introduce `--space-4` token layer for warm-paper family.
- [`creator-18px-spacing-alignment-route`](../examples/creator-manim-scene.html): CSS 18px→16px spacing alignment mutation (PR #93); aligns gap/padding 18px values to 16px (nearest @dash/tokens scale) across 4 warm-paper surfaces — manim-scene (gap:18px→16px footer grid, margin:0 0 18px→16px ticket h2), frontier-capsule (margin:0 0 18px→16px route-card h3), poster-surface (margin-bottom:18px→16px proof-card subtitle), pdf-zine (margin:18px 0 0→16px panel dark p). 11/11 measure+print PASS, 11/11 cache-integrity valid. --space-4 token layer opportunity identified for future slices.
- [`creator-spacing-token-layer-route`](../scripts/creator-surface-consistency.ts): Spacing token layer mutation (PR #96); adds `--space-4:16px` to :root across warm-paper family, replaces hardcoded 18px spacing with `var(--space-4)`. First concrete spacing grammar layer for DASH visual family. All measure+print PASS, cache-integrity PASS.
- [`creator-spacing-grammar-complete-route`](../scripts/creator-surface-consistency.ts): Spacing grammar completion mutation (PR #96 follow-on); extends `--space-4:16px` to all remaining 18px instances. Closes partial spacing-token-layer. Border-radius and custom property --h values are non-spacing visual properties.
- [`creator-bg-token-diagnostic-route`](../examples/creator-bg-token-diagnostic.html): --bg family signature diagnostic mutation (PR #95); confirms `--bg` is a dark-industrial family token — not a warm-paper token. Warm-paper surfaces (poster-surface, pdf-zine, social-card, motion-storyboard, prompt-dna, family-dna) use `--paper` for background texture. 0 warm-paper surfaces define `--bg`. 3 surfaces define `--bg`: browser-demo (`#0b1114`), remotion-scene (`#111318`), frontier-capsule (`#f3efe7`). Family signature confirmed — no addressable drift. No surface edits. Diagnostic phenotype documents --bg presence across all 11 surfaces.
- [`creator-font-family-normalize-route`](../examples/creator-family-dna.html): Font stack normalization mutation (PR #98); removes Inter from 5 surfaces (browser-demo, family-dna, p5-sketch, remotion-scene, touchdesigner-tox), aligns to ui-sans-serif...system-ui consensus shared by all other warm-paper surfaces. All measure:check PASS (5/5 surfaces), cache-integrity PASS (11/11). Eleventh concrete token-alignment step — ninth cross-surface visual grammar convergence.
- [`creator-spacing-grammar-continuation-route`](../scripts/creator-surface-consistency.ts): Spacing grammar continuation; border-radius token layer opportunity identified but not implemented. 0 shared --radius-* tokens across all 11 surfaces. Border-radius hardcoded values range 3px–9999px. do not introduce --radius-* layer without specific creator confusion.
- [`creator-type-size-audit-diagnostic-route`](../examples/creator-type-size-audit.html): Type size audit diagnostic mutation (2026-05-24); reveals h1 font-size has 10 distinct values across 11 surfaces, no shared token, tokenization requires explicit design decision before alignment. --space-4 participation: 5/6 warm-paper surfaces use it, 0/5 non-warm-paper surfaces lack it. Font-family serif/sans split is family signature, not drift. Border-radius has 0 shared tokens, --radius-* layer not recommended. No surface edits needed. creator-type-size-audit.html phenotype documents findings.
- [`creator-space-4-extension-route`](../scripts/creator-surface-consistency.ts): CSS --space-4 token extension mutation (2026-05-24); extends --space-4:16px to :root of 5 non-warm-paper surfaces (browser-demo, p5-sketch, remotion-scene, manim-scene, touchdesigner-tox). No body CSS changes. All 11 surfaces now share the same spacing token vocabulary. cache-integrity PASS (11/11), surface-health PASS (14/14).
- [`creator-radius-token-audit-route`](../examples/creator-radius-token-audit.html): Border-radius token audit diagnostic mutation (PR #104); audits border-radius usage across 12 creator surfaces. Reveals: 999–9999px near-circle (5 surfaces) and 50% circular (7 surfaces) are format-locked per surface role — cannot normalize without breaking creative intent. 8px subtle radius is the only intra-family convergence point (family-dna, motion-storyboard, prompt-dna, pdf-zine) but requires design decision before introducing --radius-card. 22–44px large radius spans 8 surfaces with no family consensus. Verdict: no --radius-* token layer viable without explicit creator design decision. creator-radius-token-audit.html phenotype documents findings.
- [`creator-family-dna-line-align-route`](../examples/creator-family-dna.html): CSS --line token alignment on family-dna (PR #108); aligns --line from rgba(244,234,214,0.12) (panel-tint alpha) to warm-paper consensus rgba(21,18,14,0.18). 9 border:var(--line) usages in grammar card now match warm-paper ink-based line color. --line converges from 8→7 distinct values across surfaces.
- [`creator-spacing-remaining-audit-route`](../examples/creator-social-card.html): Spacing grammar continuation (PR #109); aligns 3 hardcoded `padding:16px` instances to `var(--space-4)` across social-card (.proof, .footer) and motion-storyboard (.boundary). Completes the padding:16px→space-4 token alignment for warm-paper surfaces. All measure+print PASS, cache-integrity 14/14 valid.
- [`creator-font-grammar-audit-route`](../examples/creator-font-grammar-audit.html): Font grammar audit diagnostic mutation (2026-05-26); audits heading font-stack patterns across 12 creator surfaces. Reveals: 0/12 surfaces define --font-serif or --font-sans in :root. "ui-serif" is a bare token identifier with no CSS definition, used by 7 warm-paper surfaces as shorthand that falls through to Georgia/Cambria. 4 surfaces (browser-demo, family-dna, p5-sketch, remotion-scene) use Georgia directly without ui-serif wrapper. ui-serif/Georgia-direct split is token naming inconsistency, not addressable drift. Serif/sans heading split is family signature. No surface edits needed — tokenization requires design decision. creator-font-grammar-audit.html phenotype documents findings.
- [`creator-font-serif-token-layer-route`](../examples/creator-poster-surface.html): CSS --font-serif token layer mutation; adds `--font-serif: ui-serif, Georgia, Cambria, "Times New Roman", serif` to :root of 5 warm-paper surfaces (poster-surface, pdf-zine, social-card, motion-storyboard, prompt-dna-adapter), replaces all hardcoded `ui-serif, Georgia,...` stacks with `var(--font-serif)`. Eliminates naming inconsistency: bare `ui-serif` identifier now defined as a CSS token. All surface checks pass, measure+print 5/5 PASS, 14/14 cache-integrity valid after refresh.
- [`creator-font-serif-whitespace-normalize-route`](../examples/creator-motion-storyboard.html): Font-serif whitespace normalization mutation (PR #121); motion-storyboard :root `--font-serif:ui-serif,Georgia,...` (no space after colon) normalized to warm-paper consensus form `ui-serif, Georgia, ...` (space after colon). All 5 warm-paper surfaces now share identical --font-serif token value. Whitespace-only cosmetic normalization, near-zero visual risk.
- [`creator-font-sans-token-layer-route`](../examples/creator-poster-surface.html): CSS --font-sans token layer mutation; adds `--font-sans: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif` to :root of 5 warm-paper surfaces (poster-surface, pdf-zine, social-card, motion-storyboard, prompt-dna-adapter), replaces all hardcoded `ui-sans-serif, system-ui,...` stacks with `var(--font-sans)`. Mirrors --font-serif token layer pattern. All surface checks pass, 14/14 cache-integrity valid, 18/18 surface-health PASS.
- [`creator-type-stack-normalize-route`](../examples/creator-type-size-audit.html): Diagnostic phenotypes and partial spacing alignment (PR #109); type size tokenization deferred pending design decision; warm-paper font grammar completed via --font-serif (PR #116) + --font-sans (PR #123) token layers.
- [`creator-ink-token-family-explore-route`](../examples/creator-ink-family-diagnostic.html): --ink family signature diagnostic; confirms warm-paper `--ink` converges at #15120e on 9 surfaces, dark-industrial at #111318 on 2 surfaces, creative-hybrid intentionally diverged. Zero warm-paper addressable drift — family signature confirmed. No surface edits. Diagnostic phenotype documents --ink values across all 11 surfaces with family classification.
- [`creator-type-stack-normalize-v2`](../examples/creator-type-size-audit.html): warm-paper --font-sans token layer consistency fix; confirms --font-sans token layer complete across warm-paper family (poster-surface, pdf-zine, social-card, motion-storyboard, prompt-dna-adapter). Non-warm-paper surfaces intentionally diverge with hardcoded ui-sans-serif,system-ui,... stacks — family signature, not addressable drift.
- [`creator-line-panel-token-explore-route`](../examples/creator-family-dna.html): Diagnostic phenotype (PR #110); confirms `--line-panel` premise stale — warm-paper family already converged. No surface edits.
- [`creator-surface-convergence-next-route`](../scripts/creator-surface-consistency.ts): Warm-paper --space-4 body CSS audit diagnostic (2026-05-27); confirms var(--space-4) usage already complete across warm-paper family. All 12 convergence tokens stalled (0 delta). Spacing grammar opportunity exhausted for warm-paper family. No surface edits needed.
- [`creator-type-size-token-audit-route`](../examples/creator-type-size-token-audit.html): Type size token audit diagnostic (2026-05-28); audits CSS font-size px values across 12 creator surfaces. Confirms 0/12 surfaces define --text-* tokens in :root. Type size fully hardcoded, 6-11 distinct px values per surface, ~68 total unique px values. warm-paper surfaces share editorial zine role hierarchy (body 10-14px, display 40-132px) — family signature, not addressable drift. Tokenization requires explicit design decision. No surface edits recommended without designer input. Phenotype: creator-type-size-token-audit.html.
- [`creator-font-sans-non-warm-paper-extend-route`](../examples/creator-non-warm-paper-font-diagnostic.html): Non-warm-paper font family signature diagnostic; confirms 0 addressable drift across 6 non-warm-paper surfaces. Family signature confirmed.
- [`creator-font-sans-circular-fix-route`](../examples/creator-motion-storyboard.html): CSS --font-sans circular reference fix (PR #125); fixes motion-storyboard :root `--font-sans:var(--font-sans)` circular reference + body hardcoded ui-sans-serif,system-ui,... → proper `--font-sans:ui-sans-serif,system-ui,...` + body `var(--font-sans)`. First warm-paper --font-sans token layer repair.
- [`creator-font-sans-circular-fix-comprehensive-route`](../examples/creator-poster-surface.html): CSS --font-sans circular reference fix across 4 remaining warm-paper surfaces (this session); fixes poster-surface, social-card, pdf-zine, prompt-dna-adapter :root `--font-sans:var(--font-sans)` → proper `--font-sans:ui-sans-serif,system-ui,...`. Also fixes social-card `.stamp span` hardcoded `font-family:ui-sans-serif,system-ui,sans-serif` → `var(--font-sans)`. All 5 warm-paper surfaces now have correct --font-sans token layer. All surface checks PASS, measure+print 4/4 PASS, cache-integrity 14/14 valid.
- [`creator-font-sans-non-warm-paper-extend-route`](../examples/creator-non-warm-paper-font-diagnostic.html): Non-warm-paper font family signature diagnostic; confirms 6 non-warm-paper surfaces (browser-demo, family-dna, p5-sketch, remotion-scene, manim-scene, touchdesigner-tox) intentionally diverge from warm-paper font grammar with hardcoded ui-sans-serif,system-ui,... stacks. Surface grep: 0/6 non-warm-paper surfaces define --font-sans in :root. Dark-industrial and creative-hybrid families use hardcoded stacks as intentional family grammar. No surface edits needed — family signature confirmed. Diagnostic phenotype documents findings across all 11 surfaces with family classification.

## Candidate ledger
Before retaining a Darwin slice, use [`examples/creator-mutation-candidates.json`](../examples/creator-mutation-candidates.json) as the public-safe candidate ledger. It forces the run to name the observation, population, selection pressure, 3-5 single-axis candidates, one selected winner, retention level, and regression commands. Validate it with `bun creator:mutation-check`.

## Fitness rubric

| Criterion | Question | Fail state |
|---|---|---|
| Creator utility | Does it help a creator make something real faster or better? | It only explains the repo |
| Mutation clarity | Is exactly one change being tested? | Multiple uncontrolled changes |
| Artifact proof | Is there a rendered or runnable phenotype? | Only words or a dashboard screenshot |
| Memory retention | Does the idea survive future remixes? | The output cannot be resumed |
| Boundary safety | Are private inputs blocked? | Raw media, local paths, secrets, account screenshots |
| Minimal core | Does it keep frontier tools at adapter edge? | New framework before repeated wins |

## 30-day self-evolution plan

| Week | Evolution pressure | Retention target |
|---|---|---|
| 1 | Mutate creator capsule surfaces: board, poster, deck, short, PDF | one example per winning surface |
| 2 | Mutate frontier adapters: p5, Remotion, Manim, TouchDesigner, image/video models | adapter docs with proof gates |
| 3 | Mutate fitness evaluators: visual QA, contact sheets, prompt DNA checks, public-boundary checks | scripts for repeated checks only |
| 4 | Mutate agent behavior: when to choose tool, when to reject dashboard, when to promote primitive | skill/runbook updates with regression proof |

## Anti-dashboard rule

A dashboard is allowed only if it is a control surface that changes the next creative mutation.

Bad:
- a static scoreboard that says the repo is healthy;
- a gallery of pretty outputs with no mutation path;
- charts that measure activity instead of creator usefulness.

Good:
- a candidate comparison board that selects a winning workflow;
- a contact sheet that rejects bad render mutations;
- a scorecard that blocks bloated primitives from entering the core;
- a runbook that makes the next mutation cheaper and safer.

## 中文摘要

这里的 Darwin 不是“做个 dashboard”。它是自进化机制：观察 creator 的真实摩擦，生成小突变，渲染候选，按 creator usefulness 评分，选择赢家，只保留能证明有效的最小 primitive。核心保持小，frontier tools 作为 adapter 在边缘竞争。
